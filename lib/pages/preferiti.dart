import 'package:flutter/material.dart';

class Preferiti extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: SafeArea(child: Text("Preferiti")),
      ),
    );
  }
}
