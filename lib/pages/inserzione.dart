import 'package:flutter/material.dart';

class Inserzione extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Inserzione"),
      ),
      body: SafeArea(
        child: Container(
          child: Text("Inserzione x"),
        ),
      ),
    );
  }
}
